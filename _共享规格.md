# AP 展会大屏 · 四方案共享规格（实现契约）

> 这份文件是四个方案的统一标准。**所有实现必须遵守**，不确定的地方按这里写的做，
> 不要自己另发明一套。目标是：四个方案视觉差异明显，但数字、声音、合规、
> 操作方式完全一致，验收时只需要比较视觉与叙事。

---

## 0. 我们在做什么

产品 **AP（Agent Page）**：给 Shopify DTC 商家自动生成「面向 AI 的商品页 + 多语言翻译页」。
AI 爬虫（GPTBot / ClaudeBot / PerplexityBot / Google-Extended）抓取这些页面 →
消费者在 ChatGPT / Perplexity 问「买什么」时被推荐到商家店铺 → 下单成交 → 我们按佣金收费。

这块屏放在**科技展 + 商家展**，观众主要是 **DTC 商家**（也有投资人、媒体、同行）。

**核心矛盾（每个方案都要解决它）**：既要硅谷科技感 / 高级感 / 克制，
又要双十一大屏那种冲击力 / 让商家感受明显。

**已验证的解法**：
> 冲击力全部押在**体量与运动**上（主数字占屏高 30~50%、持续运动、事件到达）；
> 高级感全部押在**颜色与密度收敛**上（全屏只 2 色 + 1 个稀有色、留白 ≥ 40%、装饰归零）。
>
> 双十一大屏显 low 的部分（渐变、发光、边框、扫描线）恰好**不贡献冲击力**，所以砍掉零损失。

---

## 1. 目录与交付物

```
展会大屏播报/
├─ _shared/            公共库（禁止修改，见 §2）
│   ├─ cities.js       真实城市/市场/数据中心坐标
│   ├─ engine.js       假数据引擎 window.APEngine
│   └─ audio.js        音效引擎 window.APAudio
├─ A-任务控制台/  index.html [+ app.js …] + README.md
├─ B-引力井/      index.html [+ globe.js …] + README.md
├─ C-交易大厅/    index.html [+ …] + README.md
└─ D-诊断墙/      index.html [+ …] + README.md
```

每个方案目录必须有：
- `index.html` —— 双击能开、丢到本地服务器也能开
- `README.md` —— 设计意图 / 快捷键 / 已知取舍 / 上会前待办（不要写成实现日志）

---

## 2. 技术红线（违反其中任何一条都算不合格）

1. **不许改 `_shared/` 里的任何文件。** 四个方案共用它。需要扩展就在自己目录里写
   新文件，用 `APEngine` / `APAudio` 暴露的 API。
2. **只用 classic script**：`<script src="../_shared/engine.js"></script>`。
   **禁止** `type="module"` / `import` / `export` / `fetch()` / `XMLHttpRequest`
   —— `file://` 下这些会被 CORS 拦死，双击打不开。
3. **零网络依赖**：禁止任何 CDN、Google Fonts、远程图片、远程字体。
   展会现场经常没有可靠网络，断网必须照跑。
4. **不许用 `Math.random()`** 做画面随机。用 `APEngine.hash32(seed, salt)` 或
   `APEngine.pinkNoise(x, salt)`。原因：彩排要能复现，重启后画面要一致。
5. **时间统一走引擎**：`APEngine.update()` 每帧调一次，`APEngine.now()` 取虚拟时间，
   `APEngine.dt()` 取本帧耗时（已钳制在 100ms）。**不要自己 `Date.now()` 算差值**
   —— 锁屏恢复后浏览器会一次给你几万毫秒，不钳制则数字瞬间飙升、粒子炸开。
6. **4K 性能**：Canvas 内部分辨率锁 `1920×1080`（`canvas.width=1920`），用 CSS 拉伸到全屏，
   `devicePixelRatio` 不要乘进去。观众离屏 2 米以上看不出这层是半分辨率，
   而 4K 全分辨率软件光栅必然掉到 20fps 以下。
7. **禁止在 rAF 里** `createLinearGradient` / `createPattern` / `createBuffer`
   —— 每帧新建 GPU 侧对象，十小时后显存耗尽。启动时建好缓存。
8. **事件卡片必须对象池**：固定 8~12 个 DOM 节点循环复用。
   **禁止** append 后靠动画 `onComplete` 移除 —— 掉帧时回调不执行，节点永久留下。
9. **`will-change: transform` 只在动画期间加，结束立刻移除。** 常驻等于每个大数字
   长期占一张 4K 纹理。

---

## 3. 公共库 API 速查

```html
<script src="../_shared/cities.js"></script>
<script src="../_shared/engine.js"></script>
<script src="../_shared/audio.js"></script>
```

### APEngine

```js
APEngine.init();                  // 可选 {t0, startAt, speed, config:{...}}
APEngine.update();                // 每帧调一次，返回虚拟时间
APEngine.now() / .dt() / .t0()
APEngine.metrics()  // { pages, crawls, orders, gmv, stores,
                    //   pagesPerSec, crawlsPerSec, ordersPerMin, secondsPerOrder,
                    //   nextMilestone:{ value, remain, progress } }
APEngine.pollOrders()        // 本帧到达的订单事件数组（含人工触发）
APEngine.peekOrders(n)       // 预览未来 n 单，不消耗游标
APEngine.pollCrawls(rate)    // 本帧的爬虫事件（rate = 展示条数/秒，日志流 6~10，地球弧线 2~4）
APEngine.pollMilestones()    // 跨过里程碑时返回事件
APEngine.triggerOrder()      // 人工触发一个大单（快捷键 B 用）
APEngine.platformBreakdown() // [{platform, share}] 与线上 Platform Breakdown 同口径
APEngine.setSpeed(x) / .togglePause() / .paused / .speed
APEngine.fmtInt(n) / .fmtUSD(usd) / .fmtMoney(amount, currency, locale)
APEngine.hash32(i, salt) / .pinkNoise(x, salt) / .valueNoise(x, salt)
```

**订单事件字段**
```js
{ kind:'order', id, t, usd, amount, currency, locale,
  tier:'common'|'rare'|'epic'|'legendary', isMacro, manual,
  store:{ id, name, domain, cc, locale, currency, lang, city, lat, lon },
  product, referrer:'chatgpt.com'|…, platform:'ChatGPT'|…,
  buyerCity, buyerLat, buyerLon }
```

**爬虫事件字段**
```js
{ kind:'crawl', id, t, bot:'GPTBot'|…, platform, ip:'20.171.x.x', asn:'Microsoft Azure',
  dcCity, dcLat, dcLon, store, pageType:'product'|…, path:'/products/…',
  status:200|304, bytes, ms }
```

### 引擎已经替你保证的事（不要自己再造）
- 三个数字同源于一条漏斗，24 小时理论订单 1,489 vs 实际 1,520（差 2%），算不穿
- 客单价中位 $74 / 均值 $139，符合 DTC 品类
- 订单间隔中位 57 秒、最大 117 秒 —— **不会冷场**
- 每 10 分钟一次 3~5 单齐发的大高潮 —— **不靠运气**
- 稀有度 common 62% / rare 26% / epic 12% / legendary 少量
- 人声播报约每 2.4 分钟一次
- 计数器是时间的纯函数：断电重启不倒退、不归零，第二天接着涨（夜间降速到 15%）
- 地域一致性：荷兰店出欧元 + 荷兰语商品名 + 荷兰城市买家

### APAudio

```js
APAudio.unlock();              // 必须在一次用户手势里调（启动遮罩点击时）
APAudio.playOrder(order);      // 自动分层 / 连击音高 / 防叠加 / 决定是否人声
APAudio.playMilestone();
APAudio.blip();                // UI 轻提示
APAudio.selfTest();            // 现场开机自检
APAudio.toggleMute() / .setMuted(b) / .muted
APAudio.setAmbient(true);      // 环境音床（B 建议开）
APAudio.setHallMode(true);     // 展馆模式：切 100Hz 以下 + 2~4kHz 补 2.5dB
APAudio.setVoiceLang('zh-CN'|'en-US');
APAudio.voiceAvailable         // 机器上没有对应语音时为 false，要优雅降级
```

**人声在原型里用浏览器 `speechSynthesis` 顶着**，正式上会必须换成**预先离线合成的
音频文件**（Azure/ElevenLabs 一次性生成整句，免费额度内）—— 浏览器 TTS 的中文音色
在展会机上可能不存在或依赖联网。这一条要写进你方案的 README 待办。

---

## 4. 视觉规格

### 4.1 色板（每个方案用指定的那套，不要混）

**P1 深空石墨** —— 方案 A、B 用
```
--bg      #08090B      --panel   #101216     --hair    #1C1F25
--text    #F7F8F8      --sub     #9CA3AF     --mute    #4B5563
--crawl   #00D3A7      (爬虫，唯一冷强调)
--order   #FFB86B      (订单，唯一暖强调，稀有出现)
--grid    #7DD3FC      (经纬线，opacity .06)
```
A 以 `--crawl` 青绿为主导；B 以 `--order` 暖金为主导（同一家族，气质不同）。

**P2 暖墨陶土** —— 方案 C 用（最不像"大屏"、最像出版物/研究机构）
```
--bg      #12100E      --panel   #1B1815     --hair    #2A2521
--text    #F4EFE6      --sub     #B9AFA1     --mute    #7A7166
--accent  #D97757      --ok      #6A8E7F
```

**P3 黑绿** —— 方案 D 用（现场最亮、冲击力最强，靠"只有两色"保住克制）
```
--bg      #04120C      --panel   #0A1B12     --hair    #17301F
--text    #FFFFFF      --sub     #93A79C
--accent  #00C46A      --high    #B8FFCE     --gold    #FFD166
```

**为什么不能用彩虹渐变 / 荧光紫 / 全息蓝**：多色渐变等于没做取舍，观众读不出层级；
荧光色在 LED 屏上极易超色域被 clip 出色带；全息蓝+青+网格是 2013–2018 年免费模板的
默认皮肤，国际观众的心理索引直接是「template / 假数据」；大面积高饱和会抢走对比度预算，
让主角数字不再是画面最亮的东西 —— **冲击力被自己的装饰吃掉**。

### 4.2 字体（已实测本机可用，全部系统字体，零网络依赖）

```css
--font-display: 'Bahnschrift', 'Segoe UI Variable Display', 'Segoe UI', Inter, system-ui, sans-serif;
--font-mono:    Consolas, 'Cascadia Mono', ui-monospace, monospace;
--font-cn:      'Microsoft YaHei', 'Segoe UI', sans-serif;
```
`Bahnschrift` 是 Windows 自带的 DIN 1451 变体，技术感巨型数字的最佳选择。
UA 串 / IP / 时间戳 / 日志流一律用 `--font-mono`。

**巨型数字四条硬规则**（无论用什么实现都要守）：
```css
.bignum{
  font-family: var(--font-display);
  font-size: 220px; line-height: 220px;      /* 必须整数，非整数行高会亚像素闪烁 */
  font-variant-numeric: tabular-nums;
  font-feature-settings: "tnum" 1, "lnum" 1; /* 有字体只认后者，两个都写 */
  display: inline-flex; justify-content: flex-end; /* 进位往左长，不推右侧标签 */
  contain: layout paint;
}
```
1. 每位一个固定宽度槽（`width:1ch; overflow:hidden`），整体**右对齐** —— 位数从 7 涨到 8
   时向左生长，右侧的"页"字标签不动。
2. **只写 `transform: translate3d()`，绝不每帧改 `textContent`。** 200px 字号下改文本
   = layout + 巨型字形重光栅，是掉帧首因。
3. **单调递增 + snap 到整数**；**禁止 `back` / `elastic` 等带回弹的缓动** —— 会让数字
   **倒退**，大屏上极其难看，观众一眼看到。

   ⚠️ **补充（C 方案实测发现的走马灯陷阱）**：爬虫数每秒涨约 48，个位数轮每帧要走
   0.8 个字位。**在 60Hz 下这个速度会发生时间性别名（aliasing），肉眼看到的是数字在
   往回转** —— 正好是本条明令禁止的效果，而且它不是缓动问题，改缓动没用。
   **规则：最快的那一位必须"整格吸附"（snap），绝不做逐帧插值。** 具体两种解法都可以：
   ① 读数四舍五入到 10 或 100，让个位不动（要在旁边标 `ROUNDED TO 10`，诚实）；
   ② 个位保留但每帧只允许整格跳变。
   另外**不要**把某一位做成"持续模糊旋转"的加油机效果 —— 动起来好看，但媒体拍照和
   录屏会定格在半格状态，看起来像渲染 bug（A 方案实测踩到）。
4. 中英混排：中文字号 = 拉丁字号 × 0.92，行高 × 1.15，中英之间插 `0.15em` 间隙；
   **数字永远交给 `--font-display`**，绝不让中文字体接管数字。
   大标题禁止中英同行同大小，改「英文大字 + 中文小字副标题」两行。

### 4.3 版面

- **一个绝对主角 + 两个配角**。

> ⚠️ **2026-08-18 修订（原规格这条写错了）**：原文要求主角 cap height 达到屏高 30~50%。
> A、C 两个实现独立算出这做不到 —— 爬虫累计数带 4.2 亿基线，是 9~10 位数字 + 2 个逗号，
> 在 16:9 上几何上限就是 **17~20%**，除非放弃右栏。
> **正确的判据是宽度不是高度**：主数字应占可用栏宽的 **85~92%**，cap height 由此推导。
> 双十一那种大屏本身也是 11 位数、cap height 约 15~20% —— 冲击力来自**宽度铺满 + 持续运动**，
> 不是来自字高。所以 18% 不是妥协，是这个位数下的正解。
>
> 如果现场屏幕物理尺寸偏小（例如 55 吋电视，观看距离 5 米），再改用
> `metrics().session.crawls`（本场新增，7 位数）换取更大字号。

  主角视觉重量 45~55%，占可用栏宽 85~92%；
  配角是主角尺寸的 35~40%；订单事件区只占 15~20%（靠动效抓眼，不靠尺寸，
  这样它出现时才有惊喜感）。
- **留白 ≥ 40%**。大屏上留白是"贵"的唯一免费信号，塞满就是模板特征。
- 12 列网格，边距 = 屏宽 6%（1920 → 116px），栏间距 32px，基线对齐 8px 网格。
- **同屏绝不放三个一样大的数字** —— 三个一样大就是没有主角。

### 4.4 动效

**允许（显高级）**
| 用途 | 规格 |
|---|---|
| 数字滚动 | `cubic-bezier(.22,1,.36,1)` 600–900ms，只动变化的位数，进位位 40ms stagger |
| 地球自转 | **匀速** 0.6–1.2°/s，无缓动，永不停（匀速 = 真实天体） |
| 事件卡入场 | `cubic-bezier(.16,1,.3,1)` 480ms + 8px 位移 + opacity 0→1 |
| 事件卡退场 | `cubic-bezier(.4,0,1,1)` 240ms（慢进快退） |
| 弧线/卡片物理 | spring stiffness 170 / damping 26，**damping ratio ≥ 1，overshoot = 0** |
| 持续微动 | 噪点 0.02px/frame 漂移；脉冲 4–6s 周期呼吸，幅度 ≤ 3% |
| 弧线退场 | noise dissolve（按 UV + age 喂噪声溶解），**不要 opacity 直接淡出** |

**禁止（显廉价）**：闪烁（opacity 0↔1 < 300ms）／弹跳缩放 `cubic-bezier(.68,-.55,.27,1.55)`
／发光呼吸（box-shadow 明暗循环）／粒子四散／循环扫描线／雷达扫描扇形／数字每帧假随机跳动
／渐变自动流动／旋转+缩放同时 loop／科幻切角边框／装饰角标。

> **一条判据**：任何频率 > 1Hz 且不携带信息的动效，就是廉价。

### 4.5 远距离可读性（3–10 米）

- 主角数字 cap height ≈ 观看距离 D / 12 ~ D / 20
- 配角数字 ≥ D / 50；标签说明 ≥ D / 80；**低于 D / 120 的信息直接删掉不要放**
- 笔画粗细 = cap height 的 8~12%；**禁用 Light / Thin，最细 Medium(500)**
- 主文字对比度 ≥ 10:1（远超 WCAG 的 4.5:1，因为展厅顶光 + LED 衰减要留 1.5 倍余量）
- **5 秒法则**：0–1s 靠**运动**认出主角在涨；1–3s 读懂单位与主语（一行 ≤ 7 个汉字）；
  3–5s 被事件卡夺走注意力并理解"有人下单了"。**全屏文案总量 ≤ 25 个汉字，禁止从句。**

---

## 5. 适配与调试

设计稿基准 **1920×1080**。外层 stage 固定设计稿尺寸，用 transform scale 适配：

```js
var W = 1920, H = 1080;
function fit(){
  var s = Math.min(innerWidth / W, innerHeight / H);
  stage.style.transform = 'translate(-50%,-50%) scale(' + s + ')';
}
addEventListener('resize', fit); fit();
```
超宽屏（21:9 / 32:9）留黑边可以接受，但**如果你的布局能弹性切栏就更好**
（外层 CSS Grid + `@media (min-aspect-ratio: 21/9)` 切 2 栏/3 栏）。
`clamp()` 只用于次级文字，**不用于巨型数字**。

**必须实现 `?debug=grid`**：叠一层九宫格 + 安全区 + 中线。到场 30 秒就能确认关键数字
有没有压在 LED 拼缝上。快捷键 `G` 也切换它。

---

## 6. 统一交互（四个方案必须完全一致）

### 启动遮罩（必须有）
首屏一个全屏遮罩：产品名 + 一句话 + 「点击进入」+ 快捷键图例。
点击时执行 `APAudio.unlock()`（浏览器自动播放策略要求一次用户手势）。
遮罩上要写一行小字：`本屏数据为模拟演示数据`。

### 快捷键
| 键 | 作用 |
|---|---|
| `空格` | 暂停 / 继续（`APEngine.togglePause()`） |
| `M` | 静音 / 取消静音 |
| `B` | **人工触发一个大单**（BD 讲到关键处配合用，最重要的一个键） |
| `N` | 触发一个普通单 |
| `K` | 触发里程碑庆祝 |
| `1` `2` `3` | 速率 ×0.5 / ×1 / ×3（彩排用） |
| `G` | 调试网格 |
| `H` | 展馆音频模式（切低频 + 补人声频段） |
| `A` | 环境音床开关 |
| `F` | 全屏 |
| `R` | 重置 |
| `?` | 显示 / 隐藏快捷键图例 |

右下角放一个极小的、低对比度的提示：`? 快捷键`，不要抢画面。

---

## 7. 文案与合规（红线，逐条检查）

### 7.1 禁用词
- ❌ **「GPT 订单」/「GPT Order」** —— OpenAI 开发者条款明文禁止任何可能让第三方以为
  你的产品由 OpenAI 创建 / 支持 / 认证 / 背书，或你在与 OpenAI 合作的**表述和设计选择**。
- ✅ 改成 **「AI 引荐订单」/「AI-referred order」/「来自 AI 助手的订单」**
- ❌ 禁止出现任何第三方 **logo**（OpenAI / ChatGPT / Perplexity / Claude / Google）。
  需要图形时用中性 AI 图标 + 文字。纯文字指代是允许的。
- ✅ 爬虫 UA 名（`GPTBot` / `ClaudeBot` / `PerplexityBot` / `Google-Extended`）作为
  **技术标识符**出现在日志流里是合理指代，可以且应该保留 —— 这是"科技感"最便宜的来源。

### 7.2 必须常驻（任何一帧截图都得有，因为观众是随机时点进入的）
1. 角标：`模拟演示数据 · SIMULATED DEMO DATA`
   —— 半透明但**截图必然带上**，字高 ≥ 24px（1080p 设计稿下），全程不消失。
2. 页脚一行小字：
   > 本屏数据为产品能力演示，非真实交易记录。OpenAI、ChatGPT、Anthropic、Claude、
   > Perplexity、Google 均为其各自所有者的商标；AP 与上述公司无隶属、赞助或合作关系。
3. 产品名 `AP · Agent Page` + 一句话价值主张。

### 7.3 店铺名
一律用 `APEngine` 产出的虚构名（`Northwind Supply`、`Pine Mill`…）+ 域名 `xxx-demo.myshopify.com`。
**禁止真实品牌名**（暗示商业合作 + 虚假关联风险），**也禁止打码成 `J*** Store`**
（法律上不安全，观感更糟，BD 也没法回答"这是谁"）。

### 7.4 可以上屏的公开数据（当论据用，必须带来源域名小字）
- 「美国零售站的商品详情页，AI 可读性平均只有 **66%**」— Adobe
- 「AI 引荐访客的转化率是传统自然搜索的 **1.42×**」— Adobe, 2026-03
- 「Shopify：AI 引荐订单一年涨近 **13×**，客单价高 **14%**」— Shopify, Q1 2026
- 「约 **55%** 的 AI 引荐会话直接落在商品页，自然搜索只有 20%」

公开数据与模拟数字**必须视觉分区**（例如公开数据走浅底卡片、模拟数字走深底），
不要让真数据给假数字背书。

---

## 8. 长跑与稳定性（展会连续 8~10 小时 × 好几天）

- 内存泄漏三大元凶：事件卡 DOM 累积、`will-change` 常驻、rAF 里新建渐变对象。
- 建议做一个极简自监控：环形缓冲记 60 秒平均 FPS，掉到 40 以下时在 console 打警告
  （原型阶段不用做自动软重置，但要在 README 里写明正式版需要）。
- 所有动画用 `APEngine.dt()`，不要用固定步长。

---

## 9. 自检清单（交付前逐条跑，README 里写明结果）

- [ ] `node --check` 过所有 `.js`
- [ ] 双击 `index.html`（`file://`）能正常跑：数字在涨、事件在来、点击后有声音
- [ ] 通过本地服务器（`启动预览.bat`）也能跑
- [ ] `grep -nE "https?://|type=\"module\"|import |fetch\(|Math\.random"` 无命中
      （注释里的说明性 URL 除外）
- [ ] 巨型数字滚动时**宽度不跳、不抖、不倒退**
- [ ] 窗口从 1280×720 拉到 3840×1080，布局不炸
- [ ] `?debug=grid` 有效；`G` `空格` `M` `B` `N` `K` `1` `2` `3` `H` `A` `F` `R` `?` 全部有效
- [ ] `B` 键能立刻出一个大单 + 声音 + 人声播报
- [ ] 静音后彻底无声，取消静音后恢复
- [ ] 全屏无第三方 logo、无「GPT 订单」字样
- [ ] `模拟演示数据` 角标与页脚商标声明常驻
- [ ] 连续跑 10 分钟，console 无报错、无 DOM 节点持续增长
